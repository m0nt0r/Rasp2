<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rasp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rasp">
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReadActivity"/>
        <activity android:name=".Show" />
        <activity android:name=".MainActivity" />
        <activity android:name=".menu" />
        <activity android:name=".podgrup" />
        <activity android:name=".weekw" />
        <activity android:name=".day" />
        <activity android:name=".menu_admin" />
        <activity android:name=".kurs" />
        <activity android:name=".grup" />
    </application>

</manifest>